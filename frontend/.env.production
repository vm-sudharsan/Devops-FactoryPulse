# =============================================================================
# FACTORY PULSE — FRONTEND PRODUCTION BUILD VARIABLES
# These values are baked into the JS bundle at build time.
# Set VITE_API_URL to your actual production backend URL before building.
# =============================================================================

VITE_API_URL=https://api.yourdomain.com
VITE_APP_NAME=FactoryPulse
