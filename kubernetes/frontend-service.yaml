apiVersion: v1
kind: Service

metadata:
  name: frontend-service
  namespace: factorypulse
  labels:
    app: frontend

spec:
  type: ClusterIP

  selector:
    app: frontend

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80